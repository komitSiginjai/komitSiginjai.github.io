const state = {
	datas: [
		{
			id: 1,
			judul: 'ini jdul'
		}
	]
}

const getters = {
	allDatas: state => state.datas
}

const actions = {}

const mutations = {}

export default {
	state,
	getters,
	actions,
	mutations
}
